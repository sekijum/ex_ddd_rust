package main

import (
	"fmt"
	// "github.com/gin-gonic/gin"
	// "github.com/sekijum/go-dev-01/app/presentation/controller"
)

func main() {
	fmt.Println("hoge")
	// controller.Intro()
	// r := gin.Default()
	// r.GET("/ping", func(c *gin.Context) {
	// 	c.JSON(200, gin.H{
	// 		"message": "pong",
	// 	})
	// })
	// r.Run() // 0.0.0.0:8080 でサーバーを立てます。
}
